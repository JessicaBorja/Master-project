
#absolute path where models are stored
optim_res: False

models_dir: /home/meeso/affordance_rl/Master-project/combined/hydra_outputs/

model_name: "real_world_img_depth_dist_affMask_dense_best_train_success_10_ep"
folder_name: ${test.models_dir}/pickup/2021-09-20

eval_cfg:
  n_objects: ${n_objects}
  max_episode_length: 100